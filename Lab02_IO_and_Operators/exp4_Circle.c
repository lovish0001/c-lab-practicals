#include <stdio.h>
int main()
{
    float radius=5.6;
    float area, circumference, pi=3.14;
    area=pi*(radius*radius);
    circumference=2*pi*radius;
    printf("Area of the circle =%f\n",area);
    printf("Circumfrence of the circle =%f\n",circumference);
}
